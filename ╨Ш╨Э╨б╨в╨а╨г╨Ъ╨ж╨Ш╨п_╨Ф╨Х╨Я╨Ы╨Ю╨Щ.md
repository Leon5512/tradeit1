# 🚀 Деплой на PythonAnywhere — пошаговая инструкция

## Шаг 1 — Регистрация
1. Зайди на https://www.pythonanywhere.com
2. Нажми **"Start running Python online in less than a minute!"**
3. Выбери **"Create a Beginner account"** (бесплатно, навсегда)
4. Заполни логин, email, пароль → зарегистрируйся

---

## Шаг 2 — Загрузка файлов
1. В панели PythonAnywhere найди вкладку **"Files"**
2. Нажми **"Open Bash console here"**
3. В консоли введи команды:

```bash
# Создай папку проекта
mkdir avito_clone
cd avito_clone
```

4. Вернись во вкладку **Files**
5. Зайди в папку `avito_clone`
6. Нажми **"Upload a file"** и загрузи ВСЕ файлы из этого архива:
   - app.py
   - database.py
   - wsgi.py
   - requirements.txt
7. Создай папки вручную: **New directory** → `templates`, потом `static/uploads`
8. Зайди в `templates` и загрузи все .html файлы

---

## Шаг 3 — Установка библиотек
В Bash консоли (вкладка **Consoles**):

```bash
pip install flask werkzeug --user
```

---

## Шаг 4 — Создание веб-приложения
1. Перейди во вкладку **"Web"**
2. Нажми **"Add a new web app"** → **Next**
3. Выбери **"Flask"** → **Python 3.10** → Next
4. В поле пути к файлу замени на:
   `/home/ВАШ_ЛОГИН/avito_clone/app.py`
   (замени ВАШ_ЛОГИН на свой логин)
5. Нажми **Next**

---

## Шаг 5 — Настройка WSGI
1. На странице Web найди секцию **"Code"**
2. Нажми на ссылку WSGI-файла (что-то вроде `/var/www/ВАШ_ЛОГИН_pythonanywhere_com_wsgi.py`)
3. **Удали всё содержимое** файла
4. Вставь вместо этого:

```python
import sys
import os

project_home = '/home/ВАШ_ЛОГИН/avito_clone'

if project_home not in sys.path:
    sys.path.insert(0, project_home)

os.chdir(project_home)

from app import app as application
```

5. Замени ВАШ_ЛОГИН на свой логин
6. Нажми **Save**

---

## Шаг 6 — Запуск!
1. Вернись во вкладку **Web**
2. Нажми зелёную кнопку **"Reload"**
3. Перейди по ссылке вверху страницы:
   `https://ВАШ_ЛОГИН.pythonanywhere.com`

## ✅ Сайт работает!

---

## Если что-то пошло не так
- Вкладка **Web** → секция **"Log files"** → открой **Error log**
- Там будет написана ошибка
- Напиши её Клоду — поможем разобраться 😊
